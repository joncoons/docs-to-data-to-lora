---
apiVersion: batch/v1
kind: Job
metadata:
  name: collect-stage3-nano-nim-20260529t194029z
  namespace: runai-rag
  labels:
    app.kubernetes.io/name: completion-collector
    app.kubernetes.io/component: nemotron-nano-30b
    app.kubernetes.io/part-of: docs-to-data-to-lora
    dataset: nim-curated
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app.kubernetes.io/name: completion-collector
        app.kubernetes.io/component: nemotron-nano-30b
        app.kubernetes.io/part-of: docs-to-data-to-lora
        dataset: nim-curated
    spec:
      restartPolicy: Never
      containers:
        - name: completion-collector
          image: python:3.12-slim
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
          args:
            - |
              set -eu
              pip install --no-cache-dir -q httpx
              python /scripts/collect_completions.py \
                --dataset <DATASET_ROOT>/nim_curated/test_set_with_context.jsonl \
                --dataset-slug nim_curated \
                --model lora-nim-nemotron-nano-30b-r16 \
                --run-id "$RUN_ID" \
                --max-tokens 8192 \
                --temperature 0.01 \
                --resume \
                --max-attempts 4 \
                --retry-backoff-s 5 \
                --retry-backoff-max-s 60 \
                --concurrency 2
          env:
            - name: TARGET_API_URL
              value: "http://rag-oai-proxy.runai-rag:8080"
            - name: COMPLETIONS_OUTPUT_ROOT
              value: <EVAL_ROOT>/completions
            - name: RUN_ID
              value: "20260529T194029Z"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: collector-script
              mountPath: /scripts
              readOnly: true
            - name: nvme2
              mountPath: /workspace/artifacts
      volumes:
        - name: collector-script
          configMap:
            name: completion-collector-script
            defaultMode: 0555
        - name: nvme2
          persistentVolumeClaim:
            claimName: nvme2-pvc
---
apiVersion: batch/v1
kind: Job
metadata:
  name: collect-stage3-nano-nemo-usvcs-20260529t194029z
  namespace: runai-rag
  labels:
    app.kubernetes.io/name: completion-collector
    app.kubernetes.io/component: nemotron-nano-30b
    app.kubernetes.io/part-of: docs-to-data-to-lora
    dataset: nemo-usvcs-curated
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app.kubernetes.io/name: completion-collector
        app.kubernetes.io/component: nemotron-nano-30b
        app.kubernetes.io/part-of: docs-to-data-to-lora
        dataset: nemo-usvcs-curated
    spec:
      restartPolicy: Never
      containers:
        - name: completion-collector
          image: python:3.12-slim
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
          args:
            - |
              set -eu
              pip install --no-cache-dir -q httpx
              python /scripts/collect_completions.py \
                --dataset <DATASET_ROOT>/nemo_usvcs_curated/test_set_with_context.jsonl \
                --dataset-slug nemo_usvcs_curated \
                --model lora-nemo-usvcs-nemotron-nano-30b-r16 \
                --run-id "$RUN_ID" \
                --max-tokens 8192 \
                --temperature 0.01 \
                --resume \
                --max-attempts 4 \
                --retry-backoff-s 5 \
                --retry-backoff-max-s 60 \
                --concurrency 2
          env:
            - name: TARGET_API_URL
              value: "http://rag-oai-proxy.runai-rag:8080"
            - name: COMPLETIONS_OUTPUT_ROOT
              value: <EVAL_ROOT>/completions
            - name: RUN_ID
              value: "20260529T194029Z"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: collector-script
              mountPath: /scripts
              readOnly: true
            - name: nvme2
              mountPath: /workspace/artifacts
      volumes:
        - name: collector-script
          configMap:
            name: completion-collector-script
            defaultMode: 0555
        - name: nvme2
          persistentVolumeClaim:
            claimName: nvme2-pvc
