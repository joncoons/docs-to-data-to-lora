# Curator Dataset Experiments

This directory captures experiment-edge artifacts for the Curator and logical-entailment dataset work.

Commit policy: every experiment, training, test, or evaluation edge should end with a focused local commit that includes manifests, metrics, summaries, and reproducibility notes. Large generated datasets, caches, and model artifacts should stay out of git unless explicitly needed.
